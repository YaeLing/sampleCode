export type AttendRecord = {
    timeStamp: number
}

export type PracticeRecord = {
    state: number
    beginTime: number
    endTime: number
    practiceTime: number 
    remindCounter: number
}

export type ReportRecord = {
    timeStamp: number
}